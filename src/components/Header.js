import NavBar from './NavBar';

const Header = () => {
    return (
        <header>
            <h1> BeMan </h1>
            <NavBar inheader ={true}/>
        </header>
    )
};

export default Header;